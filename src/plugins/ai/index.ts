// AI plugin TS interface re-exports — scaffold only.
//
// Issue #95 lands only the directory layout. Issue #96 will fill the
// per-tool subdirectories with the migrated `ClaudePlugin` /
// `CopilotPlugin` impls.

export type { AiPlugin } from '../index';
