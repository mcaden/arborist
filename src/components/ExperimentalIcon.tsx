// Shared warning-triangle icon used wherever a plugin (or other feature)
// needs to be flagged as experimental in the UI. Kept presentation-only so
// each consumer controls colour (via `currentColor`), sizing (via
// `className`), and the surrounding label / tooltip semantics.

interface ExperimentalIconProps {
  readonly className?: string;
}

export function ExperimentalIcon({ className }: ExperimentalIconProps): JSX.Element {
  return (
    <svg
      xmlns="http://www.w3.org/2000/svg"
      viewBox="0 0 24 24"
      fill="none"
      stroke="currentColor"
      strokeWidth={2}
      strokeLinecap="round"
      strokeLinejoin="round"
      aria-hidden="true"
      className={className}
    >
      <path d="M10.29 3.86 1.82 18a2 2 0 0 0 1.71 3h16.94a2 2 0 0 0 1.71-3L13.71 3.86a2 2 0 0 0-3.42 0Z" />
      <line x1="12" y1="9" x2="12" y2="13" />
      <line x1="12" y1="17" x2="12.01" y2="17" />
    </svg>
  );
}
