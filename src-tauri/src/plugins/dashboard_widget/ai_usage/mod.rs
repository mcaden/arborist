//! AI Usage dashboard-widget backend module — scaffold only.
//!
//! Issue #95 lands only the directory + module declaration. Issue #98 will register an `AiUsageBackend` descriptor (with no required commands — the
//! widget reads the existing session-store) into the host's [`crate::plugins::PluginRegistry`].
//
// TODO(#98): register the AI Usage `DashboardWidgetBackend` descriptor.
