"$schema" = "../gen/schemas/desktop-schema.json"

[[permission]]
identifier = "allow-subsession-icon"
description = "Allows the frontend to query the OS application icon for a running sub-session by id."
commands.allow = [
    "subsession_icon",
]
