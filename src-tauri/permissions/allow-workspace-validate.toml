"$schema" = "../gen/schemas/desktop-schema.json"

[[permission]]
identifier = "allow-workspace-validate"
description = "Allows the frontend to validate a candidate workspace root path via the `workspace_validate` command (Roadmap §1.1)."
commands.allow = [
    "workspace_validate",
]
