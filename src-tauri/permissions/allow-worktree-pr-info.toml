"$schema" = "../gen/schemas/desktop-schema.json"

[[permission]]
identifier = "allow-worktree-pr-info"
description = "Allows the frontend to look up the pull/merge request for a worktree's current branch (Git Status dashboard) via the `worktree_pr_info` command."
commands.allow = [
    "worktree_pr_info",
]
