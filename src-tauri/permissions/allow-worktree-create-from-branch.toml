"$schema" = "../gen/schemas/desktop-schema.json"

[[permission]]
identifier = "allow-worktree-create-from-branch"
description = "Allows the frontend to create a linked git worktree from an existing branch via the `worktree_create_from_branch` command (\"From Branch\" flow)."
commands.allow = [
    "worktree_create_from_branch",
]
