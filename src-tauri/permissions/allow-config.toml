"$schema" = "../gen/schemas/desktop-schema.json"

[[permission]]
identifier = "allow-config"
description = "Allows the frontend to read and write the persisted AppConfig via `config_get` and `config_set`."
commands.allow = ["config_get", "config_set"]
