"$schema" = "../gen/schemas/desktop-schema.json"

[[permission]]
identifier = "allow-worktree-create"
description = "Allows the frontend to create a new linked git worktree under `<workspaceRoot>/.worktrees/<name>` via the `worktree_create` command (Roadmap §2.2)."
commands.allow = [
    "worktree_create",
]
