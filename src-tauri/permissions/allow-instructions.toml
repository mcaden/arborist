"$schema" = "../gen/schemas/desktop-schema.json"

[[permission]]
identifier = "allow-instructions"
description = "Allows the frontend to enumerate instruction sets via `instructions_list`."
commands.allow = ["instructions_list"]
